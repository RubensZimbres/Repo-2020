 ```
$ gcloud beta compute ssh --zone "us-central1-a" "instance-xxx" --project "machinelearning-xxx"
$ script /dev/null
$ screen
$ sudo su gus_gmail_com
$ ipython notebook.ipynb
$ screen -r
$ CTRL+C
$ exit

$ screen -X -S "sessionname" quit
``` 

```
screen -r -d 3301
```
 
